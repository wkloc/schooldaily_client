# to switch profile run with: -Dspring.profiles.active=prod
spring:
  application:
    name: schooldaily_client
  profiles:
    active: dev
  cloud:
      config:
        uri: https://localhost:8440
        username: cloudserveruser
        password: cloudserverpassword